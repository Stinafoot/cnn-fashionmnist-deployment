# ── Assignment 1: FashionMNIST Prediction API ────────────────────────────────
# Pin minor versions for reproducibility; patch updates are safe

# Web framework & server
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Deep learning (CPU wheel — smaller image, sufficient for inference)
torch==2.3.1
torchvision==0.18.1

# Image handling
Pillow==10.3.0

# Data validation (used by FastAPI response models)
pydantic==2.7.1
